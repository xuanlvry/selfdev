<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.my.test.mybatis.IUserMapper">
    <cache/>

    <!-- 查询用户信息 -->
    <select id="selectUserById" parameterType="long" resultType="com.my.test.UserInfo">
        SELECT * FROM user WHERE id = #{id}
    </select>

    <!-- 查询用户信息 -->
    <select id="selectUserByAccount" parameterType="string" resultType="com.my.test.UserInfo">
        SELECT * FROM user WHERE account = #{account}
    </select>

    <insert id="insert" parameterType="com.my.test.UserInfo" useGeneratedKeys="true" keyProperty="id">
        insert into user_data
        (account,password,name,age,tel,sex,address,updateTime,createTime)
        values
        (#{account},#{password},#{name},#{age},#{tel},#{sex},#{address},#{updateTime},current_timestamp())
    </insert>

    <update id="updateUser" parameterType="map">
        update user
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="age != null">age = #{age},</if>
            <if test="tel != null">tel = #{tel},</if>
            <if test="sex != null">sex = #{sex},</if>
            <if test="address != null">address = #{address},</if>
            updateTime = current_timestamp()
        </set>
        where id = #{id};
    </update>
</mapper>